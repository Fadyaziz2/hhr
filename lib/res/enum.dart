
enum NetworkStatus { initial , loading , success, failure,successDialog, errorDialog }

enum ActionStatus { refresh , checkInOut, mode}

enum Filter {open, close, all}

enum InternetStatus{initial ,online, offline}

