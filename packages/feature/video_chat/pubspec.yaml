name: video_chat
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  agora_uikit: 1.3.8
  # Pinned to a version of the RTC engine that retains the onExtensionError
  # callback expected by the UIKit wrapper.
  agora_rtc_engine: 6.2.1

dependency_overrides:
  agora_rtc_engine: 6.2.1